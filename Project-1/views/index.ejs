<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-6 min-h-screen">
    <div class="container mx-auto">
        <a href="/add" class="mb-8 inline-block bg-teal-500 text-white py-2 px-6 rounded-full font-semibold shadow-lg hover:bg-teal-600 transition duration-300">
            Add New Book
        </a>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
            <% Bookdata.map((el, i) => { %>
                <div class="bg-white shadow-lg rounded-lg overflow-hidden transform hover:scale-105 transition duration-300">
                    <div class="bg-teal-500 p-4">
                        <h2 class="text-xl font-bold text-white"><%= el.title %></h2>
                    </div>
                    <div class="p-6">
                        <p class="text-gray-600"><span class="font-semibold">Author:</span> <%= el.author %></p>
                        <p class="text-gray-600 mt-2"><span class="font-semibold">Published Year:</span> <%= el.publishedyear %></p>
                        <p class="text-gray-600 mt-2"><span class="font-semibold">Pages:</span> <%= el.pages %></p>
                        <p class="text-gray-600 mt-2"><span class="font-semibold">Price:</span> ₹ <%= el.price %></p>
                        <p class="text-gray-600 mt-2"><span class="font-semibold">Published Copies:</span> <%= el.publishedcopies %></p>
                        <div class="mt-4 flex justify-between">
                            <a href="/edit?id=<%= el._id %>" class="text-teal-500 hover:text-teal-700 flex items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12l-2-2m0 0l-2-2m2 2l-2 2m2-2v6m0 0H6m6 0h2m2-2a2 2 0 01-2 2m-4-2v2a2 2 0 002 2h6a2 2 0 002-2V10a2 2 0 00-2-2h-1.5a2 2 0 01-1.5-.5m0 0L11 4M9 6l-3 3" />
                                </svg>
                                Edit
                            </a>
                            <a href="/delete?id=<%= el._id %>" class="text-red-500 hover:text-red-700 flex items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                                </svg>
                                Delete
                            </a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
